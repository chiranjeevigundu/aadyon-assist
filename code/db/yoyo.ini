# yoyo-migrations config — https://ollycope.com/software/yoyo/
# The database URL is passed on the command line (the password comes from a
# Docker secret; see the `migrate` service in docker-compose.yml).
[DEFAULT]
sources = %(here)s/migrations
batch_mode = on
verbosity = 1
